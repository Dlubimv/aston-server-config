spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dlubimv/aston-server-config.git
          search-paths: src/main/resources/application.yaml
        default-label: master

server:
  port: 8888

test: true